['ant', 'bear', 'caterpillar'].pop().length; // 2

// The pop method mutates the caller array by removing
// the last element from it and returns. The length
// property then returns the length of the returned
// string 'caterpillar' which is 11.