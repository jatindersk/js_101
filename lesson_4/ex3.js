[1, 2, 3].map(num => num * num); // [1, 4, 9]

// The map method returns an array consisting of
// sqaures of each number in the original array.
// As the body of arrow function expression implemented
// has only a simple single line expression
// it implicitly returns the expression's value without
// the need of using a return keyword explicitly